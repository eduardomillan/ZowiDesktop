Nightly build.

## Commits since v0.6.0
- Add servo calibration workflow and shared calibration session model
- Expose robot command builders for gestures, mouths, melodies, and calibration
- Add CLI subcommands: calibrate, move, gesture, mouth, and sing
- Refactor PadScreen to use shared command builders instead of hardcoded strings
- Add CalibrationScreen to the GUI and wire it into Settings
- Add calibration previews, tests, and documentation updates
- Rename ZOWI_DEV to DEV_MODE and default dev mode to false in packaged builds
- Update release/version metadata and changelog for v0.6.9
